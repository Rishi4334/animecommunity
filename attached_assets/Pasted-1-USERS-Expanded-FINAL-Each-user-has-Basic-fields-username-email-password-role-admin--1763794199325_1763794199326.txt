1️⃣ USERS (Expanded, FINAL)

Each user has:

Basic fields

username

email

password

role → "admin" or "normal"

createdAt

NEW: User’s Anime/Manga Profile Links
profileLinks: {
  animeSites: [ { name, url } ],
  mangaSites: [ { name, url } ]
}


Example:

"animeSites": [
  { "name": "Aniwatch", "url": "https://aniwatch/username123" },
  { "name": "Zoro", "url": "https://zoro/user123" }
]


✔ This helps others access what platforms they use.

2️⃣ ANIME GROUP (Thread) — FINAL FORM

Each user → each anime → ONE GROUP

AnimeGroup {
  _id
  userId
  animeName
  genre
  totalEpisodes
  createdAt
  links: [ { label, url } ]  // NEW: unlimited anime links
  entries: [Entry]           // below
}

Why this is perfect:

Clean

Zero duplication

Works for start → updates → final

Stores multiple anime sources

User can update links anytime

3️⃣ ENTRIES (Opinion Timeline) — FINAL

Each entry represents:

Starting the anime

Updating

Completing

Entry {
  type: "start" | "update" | "complete"
  thoughts
  date        // user entry time
  startTime   // only for type=start
  endTime     // only for type=complete
  adminApproved: boolean
}

Why strong:

✔ You now track start date + start time
✔ You now track end date + end time
✔ Admin approves ONLY
✔ User modifies ONLY their entries
✔ Public feed only shows what admin approved

4️⃣ FLOW (Perfected)
A) User starts an anime

They submit:

animeName

genre

totalEpisodes

thoughts_start

startDate + startTime

links (1 or many)

Creates:

AnimeGroup

Entry: type="start"

Admin approves → appears in feed.

B) User adds updates

episode impression, arc change, etc
Entries are appended:

type = "update"

C) User completes anime

Entry added:

type = "complete"
thoughts_end
endDate + endTime


Admin approves → appears publicly.

5️⃣ PUBLIC FEED (Final)

Everyone can view entries.
Nobody except the original user can edit them.
Admin cannot edit.

Feed shows:

anime name

user

timeline entries

anime links

profile links

current status

Clicking an entry → opens full thread with timeline.

✔ Community can read, not touch.
✔ Clean, safe, no chaos.

6️⃣ PROFILE PAGE (Final)

User sees 2 sections:

A) Watching

AnimeGroups where last entry != complete

B) Completed

AnimeGroups where last entry is complete

Inside each:

Timeline of thoughts

Anime links

Option to add/edit links

Option to add new entry (update/complete)

✔ Nobody else can edit.
✔ Admin can't edit.

7️⃣ ADMIN PANEL (Final, Realistic)

Admin actions:

View all pending entries

Approve/reject entries

See all users

See all anime groups

See stats:

who is watching

who completed

number of entries per anime

total users

Admin cannot:

Edit entries

Edit links

Modify user content

✔ This prevents “admin overriding user opinions.”

8️⃣ TECH STACK (Confirmed)
Frontend

React

Tailwind CSS

React Router

Axios

Backend

Node.js

Express

bcrypt

JWT

Database

MongoDB Atlas

Deployment

Backend → Render

Frontend → Vercel

9️⃣ BACKEND STRUCTURE (Final)
/backend
  /src
    /config
      db.js
    /models
      User.js
      AnimeGroup.js
    /controllers
      authController.js
      animeController.js
      adminController.js
    /routes
      authRoutes.js
      animeRoutes.js
      adminRoutes.js
    /middleware
      authMiddleware.js
      roleMiddleware.js
    server.js

🔟 FRONTEND STRUCTURE (Final)
/frontend
  /src
    /components
    /pages
      Login.jsx
      Register.jsx
      Dashboard.jsx
      PublicFeed.jsx
      AnimeGroup.jsx
      AdminPanel.jsx
      UserProfile.jsx        // NEW
    /context
      AuthContext.jsx
    /utils
      api.js
    index.js
